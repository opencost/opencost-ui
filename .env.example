VITE_BASE_API_URL=""

# Clerk (browser-safe publishable key only)
VITE_CLERK_PUBLISHABLE_KEY=""
VITE_CLERK_JWT_TEMPLATE=""

# Optional — Clerk organization invite roles (Dashboard → Organizations → Roles)
# VITE_CLERK_ORG_ROLE_ADMIN="org:admin"
# VITE_CLERK_ORG_ROLE_MEMBER="org:member"

# Dev-only: skip login / treat all checks as allowed (NOT for production)
VITE_AUTH_DISABLED=""
